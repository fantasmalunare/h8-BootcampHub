<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Bootcamp</title>
  </head>
  <body>
    <h1>Form Edit Bootcamp</h1>
    <form action="/bootcamps/<%= data.id%>/edit" method="POST">
      <label for="name">Bootcamp Name:</label>
      <input type="text" name="name" value="<%=data.name%>">
      <br>
      <label for="name">Category:</label>
      <select name="category">
          <option value="" disabled selected>-select one-</option>
          <option <%=data.category === "Tech" ? "selected" : ""%> value="Tech">Tech</option>
          <option  <%=data.category === "Finance" ? "selected" : ""%> value="Finance">Finance</option>
          <option  <%=data.category === "Academic" ? "selected" : ""%>value="Academic">Academic</option>
          <option  <%=data.category === "Cooking" ? "selected" : ""%> value="Cooking">Cooking</option>
      </select>
      <br>
      <label for="fee">Fee:</label>
      <input type="number" name="fee" min="0" value="<%=data.BootcampDetail.fee%>">
      <br>
      <label for="duration">Duration (days):</label>
      <input type="number" name="duration" min="0"  value="<%=data.BootcampDetail.duration%>">
      <br>
      <label for="studentLimit">Student Limit:</label>
      <input type="number" name="studentLimit" min="0" value="<%=data.BootcampDetail.studentLimit%>">
      <br>
      <br>
      <input type="submit" value="submit">
    </form>

  </body>
</html>
