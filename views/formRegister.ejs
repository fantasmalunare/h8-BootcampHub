<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
</head>
<body>
    <h1>Form Register</h1>
        <form action="/register" method="POST">
        <label for="username">Username:</label><br>
        <input type="text" name="username" placeholder="e.g. johnwick777"><br>
        <label for="email">Email:</label><br>
        <input type="email" name="email" placeholder="e.g. johnwick@gmail.com"><br>
        <label for="password">Password:</label><br>
        <input type="password" name="password" placeholder="8 to 24 cahracters"><br>
        <br>
        <label for="bootcamp">Choose a bootcamp:</label><br>
        <select name="bootcamp">
            <option value="" disabled selected>-choose one-</option>
            <%data.forEach(x => {%>
                <option value="<%=x.id%>"><%=x.name%></option>
            <%})%>           
        </select>
        <br><br>
        <input type="submit" value="Submit">
    </form>
    <table border="1">
        <tr>
          <th>Name</th>
          <th>Category</th>
         
        </tr>
        <%data.forEach(e => {%>
        <tr>
          <td><%=e.name%></td>
          <td><%=e.category%></td>
        </tr>
        <%})%>
      </table>
</body>
</html>